/**
 * TODO: Repair this test, so selenium grid won't freeze.
 * Creator note: Somehow this test froze entire Selenium grid.
 */

/*import org.griddynamics.pages.HomePage;
import org.testng.Assert;
import org.testng.annotations.Test;

public class StoreLocationTest extends BaseTest{

    @Test
    public void findCorporateStore() {
        boolean result = new HomePage(webDriver)
                .acceptCookies()
                .getNavBar()
                .goToFindStore()
                .showStore("Żywiec");

        Assert.assertTrue(result,"Store wasn't found!");
    }

    @Test
    public void findPartnerStore() {
        boolean result = new HomePage(webDriver)
                .acceptCookies()
                .getNavBar()
                .goToFindStore()
                .showStore("Końskie Piłsudskiego");

        Assert.assertTrue(result,"Store wasn't found!");
    }
}
*/
